AWSTemplateFormatVersion: "2010-09-09"
Description: Staging ECS services — scheduled scaling (Sun-Fri 10:00-22:00 Madrid, off Saturdays)

Parameters:
  ClusterName:
    Type: String
    Default: beworking-cluster
  ScaleUpCron:
    Type: String
    Default: cron(0 10 ? * SUN-FRI *)
  ScaleDownEveningCron:
    Type: String
    Default: cron(0 22 ? * SUN-FRI *)
  ScaleDownSaturdayCron:
    Type: String
    Default: cron(0 0 ? * SAT *)
  Timezone:
    Type: String
    Default: Europe/Madrid

Mappings:
  Services:
    Backend:
      Name: beworking-staging-backend-service
    Stripe:
      Name: beworking-staging-stripe-service
    Booking:
      Name: beworking-staging-booking-service
    Dashboard:
      Name: beworking-staging-dashboard-service

Resources:

  BackendScalableTarget:
    Type: AWS::ApplicationAutoScaling::ScalableTarget
    Properties:
      ServiceNamespace: ecs
      ResourceId: !Sub
        - service/${ClusterName}/${ServiceName}
        - ServiceName: !FindInMap [Services, Backend, Name]
      ScalableDimension: ecs:service:DesiredCount
      MinCapacity: 0
      MaxCapacity: 1
      RoleARN: !Sub arn:${AWS::Partition}:iam::${AWS::AccountId}:role/aws-service-role/ecs.application-autoscaling.amazonaws.com/AWSServiceRoleForApplicationAutoScaling_ECSService
      ScheduledActions:
        - ScheduledActionName: scale-up-weekday-morning
          Schedule: !Ref ScaleUpCron
          Timezone: !Ref Timezone
          ScalableTargetAction: { MinCapacity: 1, MaxCapacity: 1 }
        - ScheduledActionName: scale-down-weekday-evening
          Schedule: !Ref ScaleDownEveningCron
          Timezone: !Ref Timezone
          ScalableTargetAction: { MinCapacity: 0, MaxCapacity: 0 }
        - ScheduledActionName: scale-down-saturday
          Schedule: !Ref ScaleDownSaturdayCron
          Timezone: !Ref Timezone
          ScalableTargetAction: { MinCapacity: 0, MaxCapacity: 0 }

  StripeScalableTarget:
    Type: AWS::ApplicationAutoScaling::ScalableTarget
    Properties:
      ServiceNamespace: ecs
      ResourceId: !Sub
        - service/${ClusterName}/${ServiceName}
        - ServiceName: !FindInMap [Services, Stripe, Name]
      ScalableDimension: ecs:service:DesiredCount
      MinCapacity: 0
      MaxCapacity: 1
      RoleARN: !Sub arn:${AWS::Partition}:iam::${AWS::AccountId}:role/aws-service-role/ecs.application-autoscaling.amazonaws.com/AWSServiceRoleForApplicationAutoScaling_ECSService
      ScheduledActions:
        - ScheduledActionName: scale-up-weekday-morning
          Schedule: !Ref ScaleUpCron
          Timezone: !Ref Timezone
          ScalableTargetAction: { MinCapacity: 1, MaxCapacity: 1 }
        - ScheduledActionName: scale-down-weekday-evening
          Schedule: !Ref ScaleDownEveningCron
          Timezone: !Ref Timezone
          ScalableTargetAction: { MinCapacity: 0, MaxCapacity: 0 }
        - ScheduledActionName: scale-down-saturday
          Schedule: !Ref ScaleDownSaturdayCron
          Timezone: !Ref Timezone
          ScalableTargetAction: { MinCapacity: 0, MaxCapacity: 0 }

  BookingScalableTarget:
    Type: AWS::ApplicationAutoScaling::ScalableTarget
    Properties:
      ServiceNamespace: ecs
      ResourceId: !Sub
        - service/${ClusterName}/${ServiceName}
        - ServiceName: !FindInMap [Services, Booking, Name]
      ScalableDimension: ecs:service:DesiredCount
      MinCapacity: 0
      MaxCapacity: 1
      RoleARN: !Sub arn:${AWS::Partition}:iam::${AWS::AccountId}:role/aws-service-role/ecs.application-autoscaling.amazonaws.com/AWSServiceRoleForApplicationAutoScaling_ECSService
      ScheduledActions:
        - ScheduledActionName: scale-up-weekday-morning
          Schedule: !Ref ScaleUpCron
          Timezone: !Ref Timezone
          ScalableTargetAction: { MinCapacity: 1, MaxCapacity: 1 }
        - ScheduledActionName: scale-down-weekday-evening
          Schedule: !Ref ScaleDownEveningCron
          Timezone: !Ref Timezone
          ScalableTargetAction: { MinCapacity: 0, MaxCapacity: 0 }
        - ScheduledActionName: scale-down-saturday
          Schedule: !Ref ScaleDownSaturdayCron
          Timezone: !Ref Timezone
          ScalableTargetAction: { MinCapacity: 0, MaxCapacity: 0 }

  DashboardScalableTarget:
    Type: AWS::ApplicationAutoScaling::ScalableTarget
    Properties:
      ServiceNamespace: ecs
      ResourceId: !Sub
        - service/${ClusterName}/${ServiceName}
        - ServiceName: !FindInMap [Services, Dashboard, Name]
      ScalableDimension: ecs:service:DesiredCount
      MinCapacity: 0
      MaxCapacity: 1
      RoleARN: !Sub arn:${AWS::Partition}:iam::${AWS::AccountId}:role/aws-service-role/ecs.application-autoscaling.amazonaws.com/AWSServiceRoleForApplicationAutoScaling_ECSService
      ScheduledActions:
        - ScheduledActionName: scale-up-weekday-morning
          Schedule: !Ref ScaleUpCron
          Timezone: !Ref Timezone
          ScalableTargetAction: { MinCapacity: 1, MaxCapacity: 1 }
        - ScheduledActionName: scale-down-weekday-evening
          Schedule: !Ref ScaleDownEveningCron
          Timezone: !Ref Timezone
          ScalableTargetAction: { MinCapacity: 0, MaxCapacity: 0 }
        - ScheduledActionName: scale-down-saturday
          Schedule: !Ref ScaleDownSaturdayCron
          Timezone: !Ref Timezone
          ScalableTargetAction: { MinCapacity: 0, MaxCapacity: 0 }

Outputs:
  StackPurpose:
    Description: Controls staging ECS desired count Sun-Fri 10-22 Madrid (Saturdays off)
    Value: !Sub Staging scaling for cluster ${ClusterName}
